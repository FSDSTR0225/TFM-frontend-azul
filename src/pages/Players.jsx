import React from "react";
import PlayerSearch from "../components/PlayerSearch"; // Ajusta la ruta según tu estructura

const Players = () => {
  return (
    <div>
      <PlayerSearch />
    </div>
  );
};

export default Players;
